apply plugin: 'kotlin'

dependencies {
    implementation Plugins.kotlin_stdlib
}

sourceSets {
    all {
        languageSettings {
            useExperimentalAnnotation('kotlin.Experimental')
        }
    }
}